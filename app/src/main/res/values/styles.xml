<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="PolarTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="colorPrimary">@color/colorAccent</item>
        <item name="colorAccent">@color/colorPrimary</item>
    </style>

    <style name="Theme.Memosplashscreen" parent="Theme.AppCompat.Light.Dialog.Alert"/>
    <style name="Theme.App.Splashscreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/redm</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/woman_meditating</item>
        <item name="postSplashScreenTheme">@style/Theme.AppCompat.Light.Dialog.Alert</item>
    </style>
</resources>
